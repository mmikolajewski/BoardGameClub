spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/gameclub?serverTimezone=UTC
  web:
    resources:
      static-locations: classpath:/static/,file:/uploads/
app:
  storage:
    location: /uploads/